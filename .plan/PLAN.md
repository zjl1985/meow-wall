# meow-wall — 视觉 + 彩蛋猫 + 生成器

## 范围（本轮）

1. **页面不一般**：像素感构图（点阵底座、专属猫区、更强 Hero/导航层次）
2. **彩蛋猫引用进来**：游行改用真实 `<Mascot/>`，并突出 Nicole / Goodman / Simon / Zero
3. **生成猫猫头**：新路由 `/studio`，调色板 + 配饰 + 表情 + 命名，保存到 localStorage，可导出 SVG

## 不做

- 不改 agent-hub 源码
- 不引入后端 / 数据库
